# Everything the harness produces lives in $CADRE_HOME, not here. These are
# only for the case where someone points CADRE_HOME at the repo.
/checkouts/
/keys/
/passes.d/
/passes.conf
/report-*.md
/shortlist-*.tsv
*.grade.json
# ...except the byte-identity goldens, which are graded artifacts on purpose.
!/tests/fixtures/byte-identity/**
/integrations/pi-review/node_modules/
